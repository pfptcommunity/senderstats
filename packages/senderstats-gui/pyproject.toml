[build-system]
requires = ['setuptools', 'setuptools-scm']
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_scheme = "post-release"
local_scheme = "node-and-date"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"senderstats_gui.images" = ["*.png", "*.ico"]

[project]
name = "senderstats-gui"
dynamic = ["version"]
readme = "README.md"
description = "Graphical user interface for senderstats"
license = { text = "MIT" }
requires-python = ">3.9"
dependencies = [
    'senderstats-core',
    'tkinterdnd'
]

[project.urls]
repository = "https://github.com/pfptcommunity/senderstats"
#homepage = "https://github.com/pfptcommunity/senderstats"
#documentation = "https://github.com/pfptcommunity/senderstats"
#changelog = "https://github.com/pfptcommunity/senderstats"

[project.scripts]
senderstats-gui = "senderstats_gui:main"
